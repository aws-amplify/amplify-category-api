// TODO: This isn't exported from the types/index, and doesn't appear to be used anywhere in the project. Verify that it is safe to remove.
export type ApiStackType = 'models' | 'http' | 'predictions' | 'function' | 'openSearch' | 'rootStack';
