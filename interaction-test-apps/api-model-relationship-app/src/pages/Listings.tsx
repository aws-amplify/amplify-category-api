import { Flex, Heading } from "@aws-amplify/ui-react";
import { NavBar } from "../NavBar";

export const Listings = () => {
  return (
    <Flex direction={'column'}>
      <NavBar />
      <Heading level={1}>Not Yet Implemented</Heading>
    </Flex>
  );
};
