version: 0.2
env:
  shell: bash
phases:
  build:
    commands:
      - source ./shared-scripts.sh && _setupNodeVersion 20
      - source ./shared-scripts.sh && _installLinux
artifacts:
  files:
    - 'shared-scripts.sh'
