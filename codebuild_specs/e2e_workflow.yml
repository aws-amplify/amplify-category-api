# auto generated file. DO NOT EDIT manually
version: 0.2
env:
  shell: bash
  compute-type: BUILD_GENERAL1_MEDIUM
batch:
  fast-fail: false
  build-graph:
    - identifier: build_linux
      buildspec: codebuild_specs/build_linux.yml
      env:
        compute-type: BUILD_GENERAL1_LARGE
    - identifier: publish_to_local_registry
      buildspec: codebuild_specs/publish_to_local_registry.yml
      env:
        compute-type: BUILD_GENERAL1_MEDIUM
      depend-on:
        - build_linux
    - identifier: rds_model_v2
      buildspec: codebuild_specs/run_e2e_tests.yml
      env:
        compute-type: BUILD_GENERAL1_SMALL
        variables:
          TEST_SUITE: src/__tests__/rds-model-v2.test.ts
          CLI_REGION: us-east-1
      depend-on:
        - publish_to_local_registry
    - identifier: rds_v2_generate_schema
      buildspec: codebuild_specs/run_e2e_tests.yml
      env:
        compute-type: BUILD_GENERAL1_SMALL
        variables:
          TEST_SUITE: src/__tests__/rds-v2-generate-schema.test.ts
          CLI_REGION: us-west-2
      depend-on:
        - publish_to_local_registry
    - identifier: cleanup_e2e_resources
      buildspec: codebuild_specs/cleanup_e2e_resources.yml
      env:
        compute-type: BUILD_GENERAL1_SMALL
      depend-on:
        - rds_v2_generate_schema
        - rds_model_v2
