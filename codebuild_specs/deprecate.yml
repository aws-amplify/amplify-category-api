version: 0.2
env:
  shell: bash
  git-credential-helper: yes
phases:
  build:
    commands:
      - git log | head -100
      - git describe --all
      - git describe --always 143a2665877e3d55592e38e33f7b6bb55df90b51 --abbrev=0
      - source ./shared-scripts.sh && _deprecate
