version: 0.2
env:
  shell: bash
phases:
  build:
    commands:
      - source ./shared-scripts.sh && _publishToLocalRegistry
  post_build:
    commands:
      - source ./shared-scripts.sh && _generateChangeLog

artifacts:
  files:
    - 'shared-scripts.sh'