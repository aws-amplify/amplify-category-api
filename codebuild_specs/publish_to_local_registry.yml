version: 0.2
env:
  shell: bash
phases:
  build:
    commands:
      - codebuild-breakpoint
      - source ./shared-scripts.sh && _publishToLocalRegistry

artifacts:
  files:
    - 'shared-scripts.sh'
