version: 0.2
env:
  shell: bash
  variables:
    NODE_OPTIONS: --max-old-space-size=8096
phases:
  build:
    commands:
      - codebuild-breakpoint
      - source ./shared-scripts.sh && _verifyAmplifyBackendCompatability
env:
  variables:
    CODEBUILD_DEBUG: "true"
