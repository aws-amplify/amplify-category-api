version: 0.2
env:
  shell: powershell.exe
phases:
  build:
    commands:
      - |
        # Use bash to run the shared Node.js setup function
        bash -c "source ./shared-scripts.sh && _setupNodeVersion 18.20.4"
      - yarn run production-build
      - yarn build-tests
